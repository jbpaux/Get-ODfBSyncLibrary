# Get-ODfBSyncLibrary
Returns all synchronized libraries in OneDrive for Business of current logged-in user

## .SYNOPSIS
Returns all synchronized libraries in OneDrive for Business of current logged-in user

## .EXAMPLE
```powershell
Get-ODfBSyncLibrary
```
